/** Moedas que cada professor recebe por semestre. */
export const MOEDAS_CREDITO_SEMESTRAL = 1000;

/** Saldo inicial do professor no extrato (mesmo valor do crédito do semestre). */
export const SALDO_INICIAL_PROFESSOR = MOEDAS_CREDITO_SEMESTRAL;
